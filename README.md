# returns

Next.js 14 (App Router) + Supabase. TypeScript.

- **Local:** `npm install` then `npm run dev`
- **Vercel:** Connect repo; env vars from project Settings → Environment Variables
- **Supabase client:** `import { supabase } from '@/lib/supabase'`
